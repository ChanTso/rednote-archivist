# Required for non-interactive runs after login.
XHS_BOARD_URL=

# Optional: keep uv cache inside this workspace in restricted sandboxes.
UV_CACHE_DIR=.uv-cache

# Optional title provider settings. Disabled by default; do not upload content unless explicitly configured.
XHS_ARCHIVE_ENABLE_LLM_TITLE=false
